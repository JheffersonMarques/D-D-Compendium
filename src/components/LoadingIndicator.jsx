import { ActivityIndicator, Dimensions } from "react-native"
import React from "react"

export const LoadingIndicator = () => {
    return (
        <ActivityIndicator color="#9600db" size={64}/>
    )
}